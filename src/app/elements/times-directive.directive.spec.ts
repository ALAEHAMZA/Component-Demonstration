import { TimesDirectiveDirective } from './times-directive.directive';

describe('TimesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TimesDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
